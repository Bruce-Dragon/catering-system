<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="com.sky.entity.Setmeal">
        insert into sky_take_out.setmeal (category_id, name, price, description, image, create_time, update_time, create_user, update_user) VALUES
            (#{categoryId},#{name},#{price},#{description},#{image},#{createTime},#{updateTime},#{createUser},#{updateUser})
    </insert>
    <update id="update" parameterType="com.sky.entity.Setmeal" useGeneratedKeys="true" keyProperty="id">
        update sky_take_out.setmeal 
        <set>
            category_id = #{categoryId},name = #{name},price = #{price},
            <if test="status != null">status = #{status},</if>
            <if test="description != null">description = #{description},</if>
            image = #{image},
        </set>
        where id = #{id};

    </update>
    <select id="page" resultType="com.sky.entity.Setmeal" parameterType="com.sky.entity.Setmeal">
        select * from sky_take_out.setmeal 
        <where>
            <if test="name != null">name = #{name}</if>
            <if test="status != null">status = #{status}</if>
        </where>
    </select>
</mapper>